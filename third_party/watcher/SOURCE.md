# wtr/watcher vendor note

- Upstream: `https://github.com/e-dant/watcher`
- Pinned reference: `release` branch, `include/wtr/watcher.hpp`
- Canonical include: `#include "wtr/watcher.hpp"`
- This repository vendors the upstream single-header C++ library at `third_party/watcher/include/wtr/watcher.hpp`; production watcher adapter code should keep this path and source note updated when refreshing the pinned header.
