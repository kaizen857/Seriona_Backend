# AGENTS.md

## 项目状态
- 当前目录就是仓库根目录；不要为调查本项目而扫描上级目录。
- 这是名为 `seriona` 的音乐播放器纯 C++ 后端项目。
- 目前项目处于空项目状态：`inc/` 和 `src/` 为空，除 `.omo/` 运行时内容外没有代码、文档或配置。
- 当前未初始化 Git，未设置 `.gitignore`，未配置 CMake，也没有可运行的构建、测试、格式化或静态检查命令。

## 目录约定
- `inc/` 预留给公开头文件。
- `src/` 预留给实现源码。
- `.omo/` 是 OpenCode 运行时内容，不应当作为项目源码或设计依据。

## 协作约束
- 本项目的文档编写以及面向用户的回答均使用中文。
- 在新增构建系统、Git 配置、代码结构或依赖前，先确认用户意图；不要把常见 C++ 项目模板当作既有约定。
- 记录命令时只写已经实际存在并验证过的命令；当前没有项目级命令可用。
