# seriona 后端设计文档

## 1. 当前阶段

本文档记录 `seriona` 音乐播放器后端的设计基线，后续会随着需求补充持续更新。

当前仓库仍处于空项目状态：`inc/` 与 `src/` 为空，尚未初始化 Git，尚未配置 `.gitignore`、CMake、构建命令、测试命令或具体依赖。本文档只描述已确认的设计目标，不把尚未讨论的工程模板当作既定方案。

## 2. 项目目标

- `seriona` 是一个音乐播放器的纯 C++ 后端项目。
- 项目采用前后端分离：后端可以脱离前端独立运行。
- 前端计划使用 QML 编写，但后端不引入任何 Qt 相关依赖。
- 最终目标支持 Windows 与 Linux 双端运行。
- 当前开发环境为 Arch Linux，KDE Plasma，Wayland 会话。

## 3. 总体架构

后端分为一个顶层控制模块和三个底层能力模块：

```text
mediaController
  ├── 文件扫描模块
  ├── 音频播放模块
  └── 元数据共享模块
```

### 3.1 mediaController

`mediaController` 是后端的顶层编排模块，负责把前端或外部控制入口的命令转换为后端内部操作，并维护当前播放器的权威状态视图。

职责：

- 接收播放、暂停、停止、切歌、跳转进度、扫描媒体库等高层命令。
- 协调文件扫描模块、音频播放模块和元数据共享模块。
- 聚合播放状态、当前曲目、播放进度、错误状态等信息。
- 对外提供稳定的控制接口和状态查询接口。

约束：

- 不直接依赖 QML 或 Qt 类型。
- 不把平台相关 API 泄露到控制层接口中。
- 不参与音频实时回调中的重负载工作。

### 3.2 文件扫描模块

文件扫描模块负责发现本地音频文件，并产出可供控制层使用的媒体条目。

职责：

- 遍历用户指定目录。
- 识别可支持的音频文件。
- 调用独立的 `TagReader` 库读取音频元数据、技术参数、歌词和封面导出路径。
- 维护 SQLite 扫描缓存，用于启动时恢复媒体库并做增量更新。
- 在运行期间监听当前音乐根目录的文件变化，更新播放列表和缓存。
- 将扫描完成、增量变化和错误信息以事件或批次结果形式回传给 `mediaController`。

待定：

- 支持的音频格式列表。
- 文件监听库选择。
- SQLite 缓存大小上限与淘汰策略。

### 3.3 音频播放模块

音频播放模块负责真实的音频加载、解码、播放控制和播放状态产生。

职责：

- 打开并播放指定音频资源。
- 使用 FFmpeg 完成音频解封装与解码。
- 使用 FFmpeg filter graph 连接解码输出与音频输出前的处理链。
- 支持播放、暂停、停止、跳转进度等基础控制。
- 支持“直出”和“混音”两种输出模式。
- 维护底层播放状态与播放时钟。
- 向 `mediaController` 发布状态变化、曲目切换、播放结束、错误等事件。

约束：

- 音频实时回调路径必须尽量短，只做确定耗时的数据搬运和必要计算。
- 不在音频实时回调中做磁盘 I/O、动态内存分配、阻塞锁、系统媒体 API 调用或前端通知。
- 需要跨线程传递的信息应通过轻量事件、原子状态或无阻塞队列转交给普通工作线程处理。

待定：

- 音频输出库。候选方向包括 `miniaudio`、`RtAudio` 和 `cubeb`，需要后续用原型验证设备释放、格式协商和 KDE/PipeWire 下的暂停行为。
- 缓冲、预读、无缝切歌和播放队列策略。

### 3.4 元数据共享模块

元数据共享模块负责把当前播放状态和媒体元数据同步给操作系统，让系统媒体控件、桌面环境或外部客户端知道当前音频流信息。

本模块在架构上视为“操作系统代理者”，与 UI 处于同一等级：它订阅 `mediaController` 的状态，也把来自操作系统媒体控件的命令下传给 `mediaController`。

职责：

- 接收 `mediaController` 发布的当前曲目、播放状态、播放能力和进度信息。
- 在 Linux 桌面环境上适配 MPRIS/D-Bus。
- 在 Windows 上适配 System Media Transport Controls 相关能力。
- 把系统媒体控件产生的播放、暂停、上一首、下一首、跳转等请求转换为内部控制命令，再交回 `mediaController`。
- 与 UI 一样只通过 `mediaController` 观察和控制播放，不直接访问音频播放模块。

约束：

- 平台 API 只允许出现在本模块的平台适配层中。
- 对 `mediaController` 暴露统一的、平台无关的接口。
- 系统交互失败不应影响核心播放链路，只应降低系统媒体集成功能。

待定：

- Windows 侧具体采用手动 SMTC 集成还是随音频后端自动集成。
- Linux 侧 D-Bus 库选择。
- 专辑封面、播放列表、歌词等扩展元数据范围。

## 4. 模块通信原则

后端内部采用“命令向下、事件向上”的通信方向：

- `mediaController` 向底层模块发送明确命令，例如播放、暂停、停止、扫描目录。
- 底层模块不直接控制其它底层模块，而是向 `mediaController` 发布事件。
- `mediaController` 依据事件更新权威状态，并决定是否通知其它模块。

这样可以保持三个底层模块彼此独立，避免文件扫描、音频播放和系统元数据共享之间形成隐式耦合。

## 5. 播放状态传递机制

### 5.1 结论

播放状态变化应优先使用事件通知机制，而不是让 `mediaController` 高频轮询音频播放模块。

轮询适合低频、非关键、可容忍延迟的状态刷新；但播放状态属于离散事件，典型变化点包括开始播放、暂停、停止、曲目切换、播放结束、seek 完成、缓冲状态变化和错误发生。对这些变化使用事件通知可以减少无效查询、降低延迟，并让控制层只在真实变化发生时更新状态。

### 5.2 推荐模型

音频播放模块对外提供两类能力：

1. 命令接口：由 `mediaController` 调用，例如 `play()`、`pause()`、`stop()`、`seek()`。
2. 事件订阅接口：由 `mediaController` 注册监听，用于接收播放状态变化。

事件建议按不可变快照传递，而不是传递可变内部对象指针。每个事件应携带单调递增版本号或产生时间戳，`mediaController` 用它丢弃过期事件，避免跨线程异步投递时旧状态覆盖新状态。基础事件可以包括：

- `PlaybackStateChanged`：播放、暂停、停止、缓冲、错误等状态变化。
- `TrackChanged`：当前曲目变化。
- `PositionDiscontinuity`：seek、切歌或底层时钟跳变导致的非连续进度变化。
- `PlaybackEnded`：当前曲目自然播放结束。
- `PlaybackError`：播放链路错误。

`mediaController` 收到事件后更新自己的状态缓存，并把需要同步的信息转发给元数据共享模块或前端接口。

### 5.3 进度信息的特殊处理

播放进度是连续变化值，不适合把每一个音频帧或每一个缓冲块都上报给 `mediaController`。推荐做法是：

- 音频播放模块维护高精度播放时钟。
- `mediaController` 持有最近一次状态快照：曲目 ID、起始位置、状态变化时间点、播放速率、是否播放中。
- 对普通 UI 显示，控制层可以基于快照推导当前进度，而不是持续查询底层。
- 发生 seek、暂停、恢复、切歌、播放结束等非连续变化时，由音频播放模块发送事件修正快照。
- 对系统媒体时间线这类外部接口，可以由元数据共享模块按平台建议做低频节流更新；例如 Windows SMTC 文档建议播放期间约每 5 秒同步一次时间线，并在暂停或跳转等状态变化时再次同步。

### 5.4 线程边界

音频播放模块内部可能存在实时音频线程、解码线程和普通控制线程。事件设计必须保护实时音频线程：

- 实时音频回调不直接执行 `mediaController` 回调函数。
- 实时音频回调只写入原子状态或无阻塞队列，必要时唤醒普通事件分发线程。
- 普通事件分发线程负责把事件投递给 `mediaController`。
- `mediaController` 的事件处理函数不得反向阻塞音频实时线程。

推荐事件流：

```text
音频实时/播放内部状态
  -> 原子状态或无阻塞事件队列
  -> 音频模块事件分发线程
  -> mediaController
  -> 元数据共享模块 / 前端接口
```

### 5.5 回调与消息队列的取舍

“回调函数”可以作为订阅接口的形式，但不建议让底层模块在任意内部线程中同步调用控制层逻辑。更稳妥的设计是事件队列或调度器：

- 对外表现为回调订阅，便于使用。
- 内部通过队列把事件切换到 `mediaController` 所属线程或主控制循环。
- 回调只接收值语义事件对象，避免生命周期问题。
- 订阅关系需要支持注销，避免模块销毁后仍被调用。

因此，本项目首选“事件通知 + 队列调度 + 状态快照”的组合，而不是直接同步回调或高频轮询。

不推荐设计成“音频播放模块每次变化时新建线程去调用 `mediaController::onTimeChange()`”。这种方式会让线程数量、事件顺序、生命周期和取消逻辑变得不可控，播放进度变化频繁时还会产生明显调度开销。更好的模型是固定一个事件消费点：

```text
audioPlayback
  -> publish(PlaybackEvent)
  -> PlaybackEventQueue
  -> mediaController 事件循环 / 单一分发线程
  -> handlePlaybackEvent(event)
```

在这个模型里，`mediaController` 不需要把大量成员函数逐个注册到底层模块。更推荐只注册一个统一事件入口，例如 `onPlaybackEvent(const PlaybackEvent& event)`，再由控制层按事件类型分发到内部处理函数。这样可以避免回调接口随着事件类型膨胀，也更容易记录、测试、重放和调试事件流。

如果后续确实需要面向多个订阅者，可以在 `mediaController` 之上或内部增加轻量事件总线，但音频播放模块仍只面向一个稳定的事件 sink 发布事件，不直接认识元数据共享模块或前端接口。

## 6. 模块详细设计

### 6.1 音频播放模块

音频播放链路按以下阶段组织：

```text
音频文件
  -> FFmpeg demux/decode
  -> FFmpeg filter graph
  -> PCM 缓冲队列
  -> 跨平台音频输出库
  -> 输出设备
```

FFmpeg 负责解封装和解码；filter graph 负责增益、采样率转换、采样格式转换、声道布局转换以及后续可能加入的音频处理。音频输出库只负责打开设备、回调取样、启动/停止流和查询设备能力，不承担解码和业务状态管理。

输出模式分为两种：

- `直出`：以当前音频文件的源采样率、采样格式和声道布局作为目标输出格式。只有当输出库和设备能接受该格式时才保持直出；如果设备不支持，必须向 `mediaController` 报告协商失败或触发明确的回退策略，不能悄悄变成混音模式。
- `混音`：以用户配置的输出采样率和采样深度作为目标格式，所有源音频都先经过 FFmpeg filter graph 统一转换后再输出。若目标格式不被设备支持，则按“向下寻找最接近的受支持格式”回退，例如 192 kHz 不可用但 96 kHz 可用时回退到 96 kHz，并向上报告实际输出格式。

输出格式协商应封装为独立步骤：

1. 收集候选采样率和采样格式。
2. 先尝试用户指定或源文件格式。
3. 失败时按同方向降级列表尝试更低但最接近的格式。
4. 成功后把实际设备格式写入播放状态快照。
5. 全部失败时产生 `PlaybackError`，由 `mediaController` 决定 UI 呈现和后续操作。

暂停语义需要区分“暂停播放状态”和“输出设备占用状态”。本项目目标是在暂停时尽量释放或停止输出设备，使 KDE/PipeWire 等桌面环境不再显示当前应用正在播放。音频输出库原型验证时必须确认：调用 stop 是否足以让系统认为音频流停止；如果不够，则需要在暂停时关闭设备、恢复播放时重新打开设备，并评估恢复延迟。

音频输出库暂不定案，当前候选比较如下：

- `miniaudio`：单头文件、小型、跨平台，支持 WASAPI、ALSA、PulseAudio、JACK 等后端，可设置采样格式、声道和采样率；旧项目已用它验证过基本方向。需要重点验证 Linux/KDE/PipeWire 暂停时的设备释放表现，以及设备格式枚举/协商能否满足直出和混音模式。
- `RtAudio`：C++ API，强调设备能力枚举、采样率列表和原生格式查询，适合做格式协商；但依赖和构建形态比单头文件方案更重。
- `cubeb`：Firefox 使用的跨平台音频库，现代桌面后端维护较强，适合低延迟与设备切换；但引入成本、构建复杂度和是否满足“轻量小型化”需要进一步评估。
- `SDL2`：暂不作为优先候选，因为暂停时释放系统播放指示和精细格式控制不符合当前目标。

旧项目中的 `AudioPlayer`、`AudioFilterChain` 可作为思路参考：它已经包含 FFmpeg filter graph、`miniaudio`、输出模式和混音参数设置等概念。但旧项目实现与 Qt 前端、旧控制层和历史状态耦合，不应直接复刻。

### 6.2 文件扫描模块

文件扫描模块内部统一使用 `std::filesystem::path` 表达路径，跨模块传递需要明确边界：内部路径对象保留平台原生表示，对外展示、缓存和跨平台逻辑使用 UTF-8 字符串。Windows 上应使用宽字符路径进入 `std::filesystem::path`，只在序列化到 SQLite 或向前端输出时转换为 UTF-8，避免把本地编码假设写死到核心逻辑里。

启动扫描流程：

```text
mediaController 提供音乐根目录
  -> 规范化 root path
  -> 查询 SQLite 中是否已有该 root 的缓存
  -> 命中缓存：先加载缓存生成播放列表快照，再扫描文件系统差异并修正缓存
  -> 未命中缓存：并行全量扫描，调用 TagReader 读取音频元数据，生成播放列表并写入缓存
```

在扫描模块内部，用户给定的音乐根目录可视为逻辑 `/`。缓存中同时保存真实绝对路径和相对逻辑路径：真实路径用于访问文件系统，相对路径用于播放列表树、移动根目录后的差异判断和 UI 展示。

SQLite 缓存设计目标：

- 支持多个不同音乐根目录的缓存。
- 每个 root 记录根路径、根路径指纹、最后扫描时间、缓存版本和统计信息。
- 每个条目记录相对路径、文件类型、最后修改时间、文件大小、TagReader 输出的元数据摘要和扫描错误。
- 数据库需要有大小上限；超限时优先淘汰最久未使用的 root 缓存或可重建的封面/派生数据。
- 数据库只作为可重建缓存，不作为唯一真相；文件系统始终是最终真相。

增量扫描规则：

- 若目录或文件的修改时间、大小或存在性未变化，可以复用缓存。
- 新增或修改的音频文件交给 `TagReader` 重新读取。
- 删除的文件从播放列表和缓存中移除。
- 无法读取或不支持的文件记录为扫描错误，但不应中断整个目录扫描。

运行时监听：

- 监听当前音乐根目录，捕获创建、删除、修改、重命名事件。
- 文件系统事件只作为“需要复查”的提示；收到事件后仍以 `std::filesystem` 重新确认当前状态。
- 对编辑器写入、下载中临时文件、批量移动等情况需要做去抖和合并，避免同一文件被短时间重复扫描。
- 变更影响音频文件时，更新 SQLite、播放列表快照，并向 `mediaController` 发布文件库变化事件。

文件监听库暂不定案。候选包括 `efsw`、`wtr/watcher` 和 `libfswatch`。选择时优先考虑 Windows/Linux 递归监听、依赖规模、C++ 集成成本、事件去抖能力和对 inotify / ReadDirectoryChangesW 的封装质量。

旧项目中的 `FileScanner` 可参考其后台扫描、协作式停止和扫描完成回调思路；但旧实现把元数据读取、封面处理、FFmpeg 技术信息和播放列表构建耦合在一起。新项目中元数据读取应由 `TagReader::Read(const std::filesystem::path&, const std::filesystem::path&)` 负责，扫描模块只负责调度、缓存和变更归并。

### 6.3 元数据共享模块

元数据共享模块作为操作系统代理者，而不是音频播放模块的一部分。它与 UI 一样位于 `mediaController` 上方：

```text
UI                    OS 元数据共享模块
 |                         |
 | 控制命令                 | 系统媒体键 / MPRIS / SMTC 控制命令
 v                         v
mediaController <--- 状态订阅 / 快照发布 ---> UI 与 OS 代理
```

来自操作系统的播放、暂停、上一首、下一首、seek、音量、循环和随机播放请求都转换为 `mediaController` 命令。`mediaController` 再通过统一状态快照把当前曲目、播放状态、播放能力、进度、音量、循环和随机播放状态发布给 UI 与 OS 代理。

Linux 适配层继续以 MPRIS/D-Bus 为目标；Windows 适配层以 SMTC 为目标。平台 API 只允许存在于本模块的适配层中。旧项目的 `SysMediaService` 可参考其“内部状态同步到 MPRIS、MPRIS 回调再下传控制命令”的结构，但旧实现依赖 Qt 项目结构、sdbus-c++ 和旧控制层接口，不能直接迁移。

### 6.4 向上通知的后续处理

本轮先确定各模块行为，向上通知机制后续再统一设计。当前已知需要向上通知的来源包括：

- 音频播放模块：播放状态、进度快照修正、曲目结束、设备格式协商结果、错误。
- 文件扫描模块：扫描进度、全量扫描完成、增量变更、扫描错误、缓存加载完成。
- `mediaController`：面向 UI 和 OS 代理发布聚合后的播放器状态。

这些通知不应各自发明一套机制。后续应统一为事件对象、状态快照和订阅接口，保持“底层模块只通知 `mediaController`，UI 与 OS 代理只订阅 `mediaController`”的边界。

## 7. 跨平台设计边界

### 7.1 Linux

Linux 桌面侧系统媒体集成优先按 MPRIS v2 设计。MPRIS 使用 D-Bus 暴露 `org.mpris.MediaPlayer2` 与 `org.mpris.MediaPlayer2.Player`，并通过 `PropertiesChanged` 信号通知播放状态和元数据变化。

对内部架构的影响：

- 内部状态模型应能表达 `Playing`、`Paused`、`Stopped`。
- 当前曲目需要稳定 ID，以便映射到 MPRIS `mpris:trackid`。
- 元数据需要能映射到 MPRIS/Xesam 字段，例如标题、艺术家、专辑、时长、封面 URI。

### 7.2 Windows

Windows 侧系统媒体集成优先按 System Media Transport Controls 设计。该机制可以显示当前媒体信息，接收系统播放控制按钮事件，并同步播放状态和时间线。

对内部架构的影响：

- 元数据共享模块需要能设置播放状态、标题、艺术家、专辑、封面和时间线。
- 系统按钮事件需要转换为内部播放命令。
- 时间线同步应节流，避免无意义高频更新。

## 8. 当前不做决定的事项

以下事项需要后续单独讨论，本文档暂不写死：

- 构建系统与目录细分。
- 音频输出库、文件监听库和 SQLite 封装方式。
- 后端对前端暴露的 IPC 或 API 形式。
- 日志、配置文件、错误码体系和测试框架。
- Windows 与 Linux 的最小支持版本。

## 9. 待确认问题

以下问题不阻塞当前文档更新，但会影响后续实现取舍：

1. 暂停时是否强制关闭输出设备，还是允许配置为“保留设备以换取更快恢复”？
2. 直出模式下，如果源格式不被设备支持，应该直接报错，还是允许用户配置为自动回退到混音模式？
3. SQLite 缓存大小上限希望按文件大小、root 数量，还是按曲目数量限制？
4. 运行时文件监听在首个可用版本中是必需能力，还是可以先以启动增量扫描为主、监听作为后续增强？
5. `TagReader` 的封面导出目录由 `seriona` 统一管理，还是由 `TagReader` 使用默认目录？

## 10. 下一步建议

下一轮设计优先明确以下问题：

1. 后端与 QML 前端之间使用什么通信方式。
2. 音频输出库优先做 `miniaudio`、`RtAudio` 还是 `cubeb` 原型验证。
3. 文件监听库优先做 `efsw`、`wtr/watcher` 还是 `libfswatch` 原型验证。
4. 是否希望播放队列、收藏、歌单等能力放入首个版本。
5. 后端进程的运行形态：命令行服务、动态库、还是独立守护进程。
