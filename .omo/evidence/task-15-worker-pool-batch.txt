Task: 15. 实现批量任务提交和结果收集
Date: 2026-06-28

Files changed:
- inc/seriona/scanner/worker_pool.h
- src/scanner/worker_pool.cpp
- tests/scanner/scanner_worker_pool_batch_tests.cpp
- tests/CMakeLists.txt
- .omo/notepads/scanner-refactor-implementation/learnings.md
- .omo/notepads/scanner-refactor-implementation/issues.md

Implementation notes:
- ScannerWorkerPool::submitBatch() now reserves pending future capacity and submits tasks in local implementation batches of 64, matching .omo/plans/scanner-refactor-implementation.md lines 613-617.
- ScannerWorkerPool::waitAll() moves pending futures out of the pool before waiting, waits until all pending futures are ready, then gets each future so task exceptions propagate while leaving the pool reusable.
- No file_scanner_orchestrator.cpp, scanner event publication, SQLite writes, perf stats, or config/error aggregation were added.

Focused test coverage:
- Submitting 100 tasks and collecting 100 results.
- Mixed batch with one throwing task; waitAll() surfaces the exception and the pool can be reused afterward.
- Reuse after a successful completed batch.
- Nontrivial concurrent task entry using a condition_variable gate instead of fragile timing assertions.

Verification:
- cmake --build build --target seriona_scanner_worker_pool_batch_tests: PASS
- ctest --test-dir build -R 'seriona.scanner.worker_pool_batch' --output-on-failure: PASS
- ctest --test-dir build -R 'seriona.scanner.worker_pool_(basic|process|batch)' --output-on-failure: PASS

Pure LOC checks:
- inc/seriona/scanner/worker_pool.h: 32
- src/scanner/worker_pool.cpp: 129
- tests/scanner/scanner_worker_pool_batch_tests.cpp: 114
- tests/CMakeLists.txt: 805, pre-existing global test registry touched only to add the required batch target and CTest alias.

Diagnostics caveat:
- No standalone lsp_diagnostics tool is exposed in this session's tool list, so changed C++ files were verified through compiler diagnostics and the focused CTest target.
