Task 18: reconcileRoot ScannerWorkerPool refactor

Changed files:
- src/scanner/file_scanner_orchestrator.cpp
- tests/scanner/scanner_service_tests.cpp
- .omo/notepads/scanner-refactor-implementation/learnings.md
- .omo/notepads/scanner-refactor-implementation/issues.md
- .omo/evidence/task-18-reconcile-root-refactor.txt

Implementation notes:
- Kept `discoverScannerPaths()` enumeration serial in `reconcileRoot()`.
- Kept `cachedSongByPath()` as the existing O(n) scan; no unordered_map or cache lookup map was introduced.
- Built `AudioReconcileTask`/`WorkerTask` batches after serial classification/hash/cache checks, submitted them through `ScannerWorkerPool::submitBatch()`, and collected `waitAll()` results.
- Preserved task-17 worker-pool failure semantics by reading `errorsSnapshot()` and continuing to publish successful songs when other worker tasks fail.
- Preserved the existing V2 `CachedSong` pipeline by using an orchestrator-private synchronized `WorkerSongStore` to keep full lyrics/user-stat data produced inside worker tag-reader tasks.
- Kept cache-hit bypass in the worker-pool path by converting an unchanged V2 cached song into a private `CachedLocation` for `WorkerTask::cachedLocation`; final song data still comes from the existing cached song so lyrics/cache save behavior is unchanged.
- Did not implement task 19 result sorting. Result publication still follows `waitAll()` future collection order from submitted entries.
- Did not implement task 20 O(1) cache lookup. Path matching still uses `cachedSongByPath()` linear lookup.

Focused coverage:
- Existing baseline: `scanner service records failures malformed lrc cancellation and preserves cache` covers TagReader failure mixed with a successful file, and confirms the successful song still publishes.
- New proof: `scanner service processes audio candidates through the worker pool` blocks one file-specific TagReader read and verifies a second audio candidate reaches TagReader before the first is released; this failed before the implementation with `reader->readCount() == 1` and passes after the worker-pool refactor.
- Focused red command/result: `ctest --test-dir build -R 'seriona.scanner_service' --output-on-failure` failed before source implementation at `tests/scanner/scanner_service_tests.cpp:423`, proving the new worker-pool-backed path was not already satisfied.
- Focused green command/result: `ctest --test-dir build -R 'seriona.scanner_service' --output-on-failure` passed after implementation.

Exact verification outputs:
- `cmake --build build --target seriona_scanner_service_tests` passed after adding the failing-first test.
- `ctest --test-dir build -R 'seriona.scanner_service' --output-on-failure` failed before implementation as expected: 1 failed doctest assertion, `CHECK( reader->readCount() == 2U )` with observed `1 == 2`.
- `cmake --build build --target seriona_scanner_service_tests` passed after implementation.
- `ctest --test-dir build -R 'seriona.scanner_service' --output-on-failure` passed after implementation: 1/1 tests passed.
- `cmake --build build --target seriona_scanner` passed after implementation: `ninja: no work to do.`
- `ctest --test-dir build -R 'seriona.scanner' --output-on-failure` passed after implementation: 24/24 tests passed.
- Readback was performed on `src/scanner/file_scanner_orchestrator.cpp` and `tests/scanner/scanner_service_tests.cpp`.
- Final stale-state rerun `cmake --build build --target seriona_scanner` passed: `ninja: no work to do.`
- Final stale-state rerun `ctest --test-dir build -R 'seriona.scanner' --output-on-failure` passed: 24/24 tests passed.

Diagnostics caveat:
- Standalone `lsp_diagnostics` is not available in this OpenCode toolset, matching inherited context. Compiler + CTest + readback are the diagnostics evidence for this task.

Adversarial checks:
- stale_state: Re-read changed implementation/tests and reran the exact required build/test commands after readback.
- dirty_worktree: `git status --short` shows unrelated existing dirty files; task-18 edits are limited to orchestrator, scanner service test, and required `.omo` artifacts.
- misleading_success_output: Captured the failing-first service test before implementation and the broad scanner CTest pass after readback.
- malformed_input: Existing scanner service test continues to cover malformed LRC plus TagReader failure mixed with a successful file; broad scanner service and scanner regex passed.
- flaky_tests: New test uses condition-variable synchronization and observable reader count/snapshot state, not a timing-only completion assertion.

TODO/FIXME/HACK scan:
- `grep` found no TODO/FIXME/HACK in the changed source/test/evidence/notepad files. Matches are limited to `docs/scanner-concurrency-recommendation.md` and third-party headers.

Pure LOC counts:
- `src/scanner/file_scanner_orchestrator.cpp`: 771 pure LOC after task 18.
- `tests/scanner/scanner_service_tests.cpp`: 386 pure LOC after task 18.
- Both are existing oversized files; this task did not split them because the requested scope was only worker-pool integration.

Residual risk:
- `ScannerWorkerPool::waitAll()` currently returns futures in submission order, but task 19 owns explicit result ordering. This task did not add a stable sorting layer.
- The private `CachedLocation` conversion is only for invoking worker-pool cache-hit bypass; production cache persistence still uses the existing V2 `CachedSong` path.
