Task: 16. 实现性能统计（原子累加和 Phase breakdown）
Date: 2026-06-28

Files changed:
- inc/seriona/scanner/worker_pool.h
- src/scanner/worker_pool.cpp
- tests/scanner/scanner_worker_pool_stats_tests.cpp
- tests/CMakeLists.txt
- .omo/notepads/scanner-refactor-implementation/learnings.md
- .omo/notepads/scanner-refactor-implementation/issues.md

Implementation:
- Added WorkerPoolStatsSnapshot with non-atomic snapshot values: submittedTasks, cacheHits, scannedFiles, tagReaderTime, hashTime.
- Added ScannerWorkerPool::statsSnapshot() as the stable reporting/assertion API.
- Added formatWorkerPoolStatsBreakdown() for worker-pool-local phase/per-file reporting.
- Added relaxed atomic counters/timers inside ScannerWorkerPool::Impl; worker threads update cache-hit and tag-reader timing stats without mutexes on the hot path.
- Hash timing remains exposed as zero because current worker-pool task processing does not perform file hashing yet.

Focused coverage:
- 100 submitted tasks with 80 cached locations yields cacheHits == 80 and scannedFiles == 20.
- Multiple tag-reader tasks accumulate positive tagReaderTime without exact timing assumptions.
- Stats snapshots remain reusable after multiple batches and counters accumulate across reuse.
- Breakdown formatting asserts structural fields only.

Verification:
- cmake --build build --target seriona_scanner_worker_pool_stats_tests && ctest --test-dir build -R 'seriona.scanner.worker_pool_stats' --output-on-failure
  Result: passed. CTest ran seriona.scanner.worker_pool_stats, 1/1 passed.
- Pure LOC checks:
  inc/seriona/scanner/worker_pool.h: 41
  src/scanner/worker_pool.cpp: 170
  tests/scanner/scanner_worker_pool_stats_tests.cpp: 96
  tests/CMakeLists.txt: 818 existing monolithic registry; touched minimally for required target and alias.

Diagnostics caveat:
- This OpenCode toolset does not expose a standalone lsp_diagnostics tool in the available tool list. apply_patch inline diagnostics initially reported stale/unknown WorkerPoolStatsSnapshot in worker_pool.cpp after the header edit, but the exact CMake target build compiled cleanly after CMake regeneration.

Scope notes:
- Did not touch src/scanner/file_scanner_orchestrator.cpp.
- Did not add event publication, SQLite writes, cancellation policy, error aggregation, or task-17 config tuning.
