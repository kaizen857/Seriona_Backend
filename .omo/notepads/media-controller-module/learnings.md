
[2026-06-22 00:00 UTC] 控制层公共契约扩展采用纯值类型与前向声明组合；`MediaControllerDependencies` 直接引用 `audio::AudioPlaybackService`、`scanner::FileScannerService`，`metadata::MetadataSharingService` 只保留为前向声明的 `unique_ptr`，避免 `control` 与 `metadata` 头形成环依赖。
[2026-06-22 00:00 UTC] `inc/seriona/control/control_contracts.h` 只新增控制层快照、通知、错误、命令结果、选项和依赖契约；现有 `PlayerStateSnapshot`、`MediaControlCommand`、`SubscriptionHandle` 保持原有布局与用法不变。
[2026-06-22 00:00 UTC] 控制层 fake harness 采用 `DeterministicClock`、值收集器和三类 fake service；订阅句柄通过 `weak_ptr` + 原始指针注销路径实现一次性释放，便于后续 contract tests 验证重复 `unsubscribe()` 幂等性。
[2026-06-22 00:00 UTC] `tests/control/control_contract_tests.cpp` 里要避免用 `audio`/`scanner`/`metadata` 作为局部变量名，否则会遮蔽命名空间；这类 contract tests 更适合显式命名 `fakeAudio`、`fakeScanner`、`fakeMetadata` 之类的对象名。
[2026-06-22 00:00 UTC] Task 4 的 `ControlEventLoop` 保持在 `src/control` 内部：生产模式 `start()` 创建单 worker 串行消费队列，测试 inline 模式不启动 worker 且只由 `drainForTests()` 执行已投递任务；`stop()` 标记停止并丢弃未执行队列，确保析构期间不会运行捕获已销毁对象的任务。
[2026-06-22 00:00 UTC] Task 4 的 `SubscriptionStore` 使用单调递增 `SubscriptionHandle::subscriptionId`、weak-state unsubscribe 和快照式订阅者 ID 遍历；回调执行前重新取 callback，因此回调期间显式 unsubscribe 不会破坏遍历，订阅者异常会累计到 `SubscriptionExceptionReport` 并继续通知后续订阅者。
[2026-06-22 00:00 UTC] Task 5 的 `ControlStateReducer` 作为纯状态机放在 `src/control` 私有实现中：命令、音频事件、扫描事件只归并 `PlayerStateSnapshot`/`LibraryStateSnapshot` 并返回 `ControlIntent`/领域通知，后续 facade 再负责执行音频/扫描服务调用；`seriona_media_controller_tests` 现在改为 reducer 语义测试源，覆盖无库播放、曲目选择、skip/repeat/shuffle、seek/volume clamp、stale event、扫描生命周期和播放结束策略。
[2026-06-22 11:11 UTC] Task 6 final-wave 修复后，`seriona_control` 不再吸收 audio implementation `.cpp` 或 FFmpeg 链接；`media_controller_module.cpp` 的缺省 audio 依赖使用 control 私有 no-op `AudioPlaybackService`，真实 audio 服务应由上层显式注入。
[2026-06-22 11:11 UTC] `MediaController` 构造函数安装 audio/scanner sinks，因此 `shutdown()` 必须即使从未 `start()` 也清空 service-side sinks；metadata `stop()` 仍只在已 start 场景调用，避免未启动析构留下捕获 `this` 的 callback。
[2026-06-22 00:00 UTC] Task 6 重新收紧 `seriona_media_controller_tests`：测试文件不再 include `src/control/control_state_reducer.h`，`tests/CMakeLists.txt` 也移除 `src/control` 私有 include，确保 `seriona.control_controller` 只能通过公开 `MediaController` facade 和 fake audio/scanner/metadata 链路验证集成行为。
[2026-06-22 00:00 UTC] `MediaController` 的 metadata 命令回调必须只 post 到 `ControlEventLoop`；fake metadata `emitCommand()` 后、`drainForTests()` 前音频 fake 计数应保持不变，drain 后才执行 reducer intent 并调用 `loadTrack`/`play`。
[2026-06-22 00:00 UTC] Task 7 的 shutdown 生命周期需要同时覆盖“回调入口”和“已排队任务执行点”：先将 `stopping_` 置位并清空 audio/scanner sink、退订 metadata callback、调用 metadata `stop()`，再由 `ControlEventLoop::stop()` 丢弃残留队列；audio/scanner/metadata late callback 即使被 fake 触发也不应再改变最终 snapshot 或触发 metadata update。
[2026-06-22 00:00 UTC] facade 级异常隔离可直接依赖 `SubscriptionStore`：首个 player/library/domain subscriber 抛异常时，后续 subscriber 仍会收到已提交快照/通知，且 `publishPlayerSnapshot()` 之后的 metadata update 仍应完成。
[2026-06-22 00:00 UTC] Task 8 facade 回归补齐后，`seriona_media_controller_tests` 覆盖命令拒绝通知、SeekTo/SeekBy clamp、volume clamp、mute/toggle 转发、SkipNext/repeat-all/repeat-one playback-ended、stale audio/scanner event 忽略，以及 scanner error 到 library snapshot/domain notification 的发布路径；仍保持只经公开 `MediaController` 和 fake service 验证。
[2026-06-22 00:00 UTC] Task 8 边界 grep `rg -n "Qt|QML|sdbus|dbus|windows.h|MPRIS|SMTC|TagReader|SQLite|miniaudio" inc/seriona/control src/control tests/control` 只有 `tests/control/control_contract_tests.cpp` 的 forbidden-token 守卫字符串命中，生产 `inc/seriona/control` 与 `src/control` 无命中。
